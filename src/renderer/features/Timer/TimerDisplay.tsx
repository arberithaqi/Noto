// Platzhalter für die Timer/Pomodoro Anzeige-Komponente (Renderer)
import React from 'react';

const TimerDisplay: React.FC = () => {
  return (
    <div>
      <h2>Timer / Pomodoro</h2>
      {/* Hier kommt die Timer-Anzeige hin */}
    </div>
  );
};

export default TimerDisplay; 